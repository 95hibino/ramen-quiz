/**
 * 旧 Supabase スタブ。
 * Phase 2 で `supabaseClient.ts` に実装を移したため、本ファイルは
 * import パスの後方互換のためだけに残してある。新規コードは
 * `@/lib/supabaseClient` を使うこと。
 */
export { getSupabaseClient, isSupabaseConfigured } from './supabaseClient';
